<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Isotretinoin2" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/ash_grey</item>
        <item name="colorOnPrimary">@color/sun_glow</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/sun_glow</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="TipsTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/melonish</item>
        <item name="android:fontFamily">@font/fontpoppins</item>

    </style>
</resources>